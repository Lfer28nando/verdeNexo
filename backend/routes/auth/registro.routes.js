import { Router } from "express";

const router = Router();

// TODO: Implementar rutas de registro
// Placeholder routes para evitar errores de import

router.post("/", (req, res) => {
  res.json({ 
    ok: true, 
    message: "Registration routes not implemented yet" 
  });
});

export default router;
